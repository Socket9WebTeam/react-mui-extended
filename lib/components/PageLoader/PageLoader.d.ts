import { ReactElement } from 'react';
/**
 * A Full-size loader
 * Note: It will only take up all vertical space if the parent is a flex box in column direction*
 */
declare const PageLoader: () => ReactElement;
export default PageLoader;
