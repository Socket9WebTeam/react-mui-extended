import { ReactElement } from 'react';
export type ButtonLoader = {
    isLoading?: boolean;
};
declare const ButtonLoader: ({ isLoading }: ButtonLoader) => ReactElement | null;
export default ButtonLoader;
